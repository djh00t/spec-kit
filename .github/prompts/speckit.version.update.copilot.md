# /version-update — GitHub Copilot

## Command Purpose

Guide users through updating versioning tables in spec-kit governance files (constitution.md, testing/*.md, and custom policies in `.specify/memory/`).

...existing code...